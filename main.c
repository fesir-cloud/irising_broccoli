inculde:<iostream>

//array mind(n,m,i,x) //vierdimensionaler Array der die Gewichte speichert

void main {
//load initial image(w x h Pixels)
//spread image pixelated over matrix (w x h) <- array

for(int n, n < w, n++){

  for(int m, m < h, n++){
  //Define Perceptron (n|m)
  //output pixel (n|m) HEX Value to Perceptron (n|m) as Parameter
    }
  }

  
}

class node(int hexR; int hexG;int hexB;) {
//int a=f(n,m,i,1)
//int a=f(n,m,i,1)
  //int hexR;
  //int hexG;
  //int hexB;
//Verarbeitung  
}

