



void main {
#load initial image(w x h Pixels)
#spread image pixelated over matrix (w x h) <- array

for(int n, n < w, n++){
#output pixels HEX Values to Perceptrons
  for(int m, m < h, n++){
    }
  }

  
}
