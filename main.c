inculde:<iostream>

//array mind(n,m,i,x) //vierdimensionaler Array der die Gewichte speichert

void main {
//load initial image(w x h Pixels)
//spread image pixelated over matrix (w x h) <- array

for(int n=1, n < w, n++){

  for(int m=1, m < h, n++){
  //Define Perceptron (n|m)
  //output pixel (n|m) HEX Value to Perceptron (n|m) as Parameter
    }
  }

  
}

//class node(int n, int m, int l, int hexR; int hexG;int hexB;) {                    //node soll noch indizes n m und l bekommen
//int Weighta=mind(n,m,l,1)
//int Weightb=mind(n,m,l,2)
//int Weightc=mind(n,m,l,3)
  //int hexR;
  //int hexG;
  //int hexB;
public:
;

//Verarbeitung aller HexX mit des Weights...und Output des Ergebnittes an node(n,m,l+1)

}

